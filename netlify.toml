[build]
  command = "bundle install && bundle exec jekyll build --config _config.yml,_config_netlify.yml --future"
  publish = "_site"

[build.environment]
  RUBY_VERSION = "3.3.4"
  JEKYLL_ENV = "production"
  BUNDLE_GEMFILE = "Gemfile.netlify"
